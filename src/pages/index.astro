---
import Base from '../layouts/Base.astro';
import { SITE, withBase } from '../config/site';
---

<Base>
  <section class="hero-fs">
    <div id="robot-stage" class="hero-fs-stage">
      <p class="stage-loading">inicializando robot…</p>
    </div>

    <div class="stage-corners" aria-hidden="true"></div>

    <div class="hero-fs-content">
      <div class="hero-fs-top anim-fade-up delay-1">
        <div class="sys-badge">
          <span class="pulse-dot"></span>
          <span>challenge jar · rosario · 03–06 nov 2026</span>
        </div>
        <h1 class="hero-fs-title anim-fade-up delay-2">
          Un robot.<br />
          Una tarea.<br />
          <span class="grad-text">Tu código.</span>
        </h1>
      </div>

      <div class="hero-fs-bottom">
        <div class="hero-fs-info anim-fade-up delay-3">
          <p class="hero-fs-sub">
            El AIR Club UdeSA construyó una flota de ROSMASTER X3 y su propio simulador.
            Los equipos programan su comportamiento autónomo contra el gemelo digital —
            y la tarea exacta se revela en la JAR.
          </p>
          <div class="hero-fs-actions anim-fade-up delay-4">
            <a class="btn btn-primary btn-glow" href={withBase('/setup/')}>
              Preparar el entorno
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" class="btn-arrow"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
            </a>
            <a class="btn btn-ghost" href="#participar">Cómo participar</a>
          </div>
        </div>

        <div class="hero-fs-meta anim-fade-up delay-4">
          <span class="stage-hint">
            <span class="pulse-dot"></span>arrastrá para orbitar · click + scroll para zoom
          </span>
          <a class="scroll-hint" href="#desafio" aria-label="Bajar a la siguiente sección">
            <span>scroll para explorar</span>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14"/><path d="m19 12-7 7-7-7"/></svg>
          </a>
        </div>
      </div>
    </div>
  </section>

  <div class="strip" id="desafio">
    <span class="strip-label"><span class="pulse-dot"></span>próximo evento</span>
    <span class="strip-event">JAR 2026 · Rosario · 03–06 NOV</span>
    <a class="strip-link" href="#participar">cómo participar ↓</a>
  </div>

  <section class="sec">
    <div class="wrap">
      <div class="reveal">
        <p class="sys-label">[ 01 // el desafío ]</p>
        <h2 class="s-title">Comportamiento autónomo,<br />de verdad.</h2>
        <p class="s-desc">
          No hay control remoto: los robots tienen que decidir solos. Cada universidad inscribe
          un equipo que desarrolla software contra nuestro simulador y compite en Rosario
          ejecutando ese mismo código en los robots reales.
        </p>
      </div>
      <div class="grid-3">
        <div class="card reveal reveal-d1">
          <div class="icon-box">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="4" width="16" height="16" rx="2"/><rect x="9" y="9" width="6" height="6"/><path d="M9 1v3m6-3v3M9 20v3m6-3v3M1 9h3m-3 6h3m16-6h3m-3 6h3"/></svg>
          </div>
          <h3>El robot lo pone el club</h3>
          <p>ROSMASTER X3 con ruedas mecanum de movimiento omnidireccional, LiDAR 2D, cámara RGB-D e IMU. Vos solo traés el software.</p>
        </div>
        <div class="card reveal reveal-d2">
          <div class="icon-box">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8m-4-4v4"/></svg>
          </div>
          <h3>Gemelo digital público</h3>
          <p>Simulador propio sobre Gazebo Fortress con física real de contactos. Mismos sensores, mismos topics que el robot físico: desarrollás a distancia contra el mismo entorno.</p>
        </div>
        <div class="card reveal reveal-d3">
          <div class="icon-box">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
          </div>
          <h3>La tarea es secreta</h3>
          <p>Revelaremos esta información pronto. Mientras tanto, los workshops te preparan con las herramientas base para resolver cualquier comportamiento.</p>
        </div>
      </div>
    </div>
  </section>

  <div class="divider"></div>

  <section class="sec" id="participar">
    <div class="wrap">
      <div class="reveal">
        <p class="sys-label">[ 02 // participación ]</p>
        <h2 class="s-title">De cero a competir</h2>
        <p class="s-desc">
          Bajamos la barrera de entrada al mínimo: no hace falta hardware propio ni experiencia
          previa en robótica. Seguí el camino y llegás.
        </p>
      </div>
      <div class="grid-3">
        <div class="card reveal reveal-d1">
          <div class="step-n">$ 01</div>
          <h3>Montá tu entorno</h3>
          <p>Ubuntu 22.04 + ROS 2 Humble + Gazebo Fortress con nuestra guía paso a paso, y el simulador corriendo en tu compu.</p>
        </div>
        <div class="card reveal reveal-d2">
          <div class="step-n">$ 02</div>
          <h3>Seguí los workshops</h3>
          <p>Uno por semana, asincrónicos: desde un talker y un listener hasta SLAM y navegación. Código listo para correr en el sim.</p>
        </div>
        <div class="card reveal reveal-d3">
          <div class="step-n">$ 03</div>
          <h3>Competí en Rosario</h3>
          <p>Nos enviás tu repositorio antes del evento, lo verificamos y lo ejecutamos en los robots reales durante la JAR.</p>
        </div>
      </div>
    </div>
  </section>

  <div class="divider"></div>

  <section class="sec">
    <div class="wrap">
      <div class="reveal">
        <p class="sys-label">[ 03 // recursos ]</p>
        <h2 class="s-title">Empezá por acá</h2>
        <p class="s-desc">Todo lo que necesitás para llegar listo a noviembre.</p>
      </div>
      <div class="grid-3">
        <a class="card reveal reveal-d1" href={withBase('/setup/')}>
          <span class="tag">guía</span>
          <h3 style="margin-top:14px">Setup</h3>
          <p>Instalá ROS 2 Humble, Gazebo Fortress y RViz, y levantá el simulador del ROSMASTER paso a paso.</p>
        </a>
        <a class="card reveal reveal-d2" href={withBase('/workshops/')}>
          <span class="tag">semanal</span>
          <h3 style="margin-top:14px">Workshops</h3>
          <p>Talkers, listeners, movimiento mecanum, SLAM: mini-proyectos asincrónicos para dominar las bases.</p>
        </a>
        <a class="card reveal reveal-d3" href={SITE.simRepo} target="_blank" rel="noopener">
          <span class="tag tag-muted">open source</span>
          <h3 style="margin-top:14px">Simulador</h3>
          <p>El gemelo digital completo está en GitHub. Mirá el código, abrí issues, metete adentro.</p>
        </a>
      </div>
    </div>
  </section>
</Base>

<script>
  const els = document.querySelectorAll('.reveal');
  const io = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          io.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.12 }
  );
  els.forEach((el) => io.observe(el));

  document.querySelectorAll<HTMLElement>('.card').forEach((card) => {
    card.addEventListener('pointermove', (e) => {
      const rect = card.getBoundingClientRect();
      card.style.setProperty('--mx', `${e.clientX - rect.left}px`);
      card.style.setProperty('--my', `${e.clientY - rect.top}px`);
    });
  });
</script>

<script>
  import * as THREE from 'three';
  import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
  import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';

  const stage = document.getElementById('robot-stage');
  const hint = stage?.querySelector<HTMLElement>('.stage-loading') ?? null;

  function reportError(msg: string) {
    if (!hint) return;
    hint.classList.remove('done');
    hint.textContent = `error: ${msg}`;
  }

  if (stage && hint) {
    window.addEventListener('error', (e) => reportError(e.message));
    window.addEventListener('unhandledrejection', () => reportError('promesa rechazada'));

    const base = import.meta.env.BASE_URL.replace(/\/+$/, '');
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    let started = false;

    const lazyLoad = new IntersectionObserver(
      (entries) => {
        if (!started && entries.some((entry) => entry.isIntersecting)) {
          started = true;
          lazyLoad.disconnect();
          try {
            initViewer();
          } catch (err) {
            reportError(err instanceof Error ? err.message : String(err));
          }
        }
      },
      { rootMargin: '300px' }
    );
    lazyLoad.observe(stage);

    function initViewer() {
      if (!stage) return;

      const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
      renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
      renderer.outputColorSpace = THREE.SRGBColorSpace;
      renderer.toneMapping = THREE.ACESFilmicToneMapping;
      renderer.toneMappingExposure = 1.25;
      stage.prepend(renderer.domElement);

      const scene = new THREE.Scene();
      const camera = new THREE.PerspectiveCamera(34, 1, 0.05, 50);
      camera.position.set(1.75, 1.05, 2.25);

      // Iluminación para resaltar materiales metálicos y reflejos
      scene.add(new THREE.HemisphereLight(0xffffff, 0x14281c, 2.6));
      
      const key = new THREE.DirectionalLight(0xffffff, 3.4);
      key.position.set(2.5, 4.5, 3.5);
      scene.add(key);

      const fill = new THREE.DirectionalLight(0x4ade80, 1.8);
      fill.position.set(-3.5, 2.5, 2.5);
      scene.add(fill);

      const backLight = new THREE.DirectionalLight(0x86efac, 1.4);
      backLight.position.set(0, 3.5, -4);
      scene.add(backLight);

      const rim = new THREE.DirectionalLight(0xfbbf24, 1.1);
      rim.position.set(3.5, 1.8, -2.5);
      scene.add(rim);

      const groundBounce = new THREE.DirectionalLight(0x22c55e, 0.8);
      groundBounce.position.set(0, -3, 0);
      scene.add(groundBounce);

      const controls = new OrbitControls(camera, renderer.domElement);
      controls.enableDamping = true;
      controls.enablePan = false;
      // Desactivamos el zoom automático por rueda para permitir el scroll natural de la página
      controls.enableZoom = false;
      controls.autoRotate = !reducedMotion;
      controls.autoRotateSpeed = 1.2;
      controls.target.set(0, -0.04, 0);

      // Manejador para permitir zoom con rueda SOLO cuando el botón del mouse está presionado
      let isMouseDown = false;
      stage.addEventListener('pointerdown', (e) => {
        if (e.button === 0) isMouseDown = true;
      });
      window.addEventListener('pointerup', () => {
        isMouseDown = false;
      });
      window.addEventListener('pointercancel', () => {
        isMouseDown = false;
      });

      stage.addEventListener(
        'wheel',
        (e) => {
          if (isMouseDown) {
            e.preventDefault();
            const zoomFactor = Math.pow(0.95, -e.deltaY * 0.008);
            const offset = camera.position.clone().sub(controls.target);
            const dist = offset.length();
            const newDist = dist * zoomFactor;
            if (newDist >= 0.7 && newDist <= 5.5) {
              offset.multiplyScalar(zoomFactor);
              camera.position.copy(controls.target).add(offset);
              controls.update();
            }
          }
        },
        { passive: false }
      );

      const resize = () => {
        const w = stage.clientWidth;
        const h = stage.clientHeight;
        renderer.setSize(w, h);
        camera.aspect = Math.max(w, 1) / Math.max(h, 1);

        // En desktop desplazamos el punto de vista para que el robot se ubique en la mitad derecha
        if (w >= 1024) {
          camera.setViewOffset(w, h, -Math.round(w * 0.22), 0, w, h);
        } else {
          camera.clearViewOffset();
        }
        camera.updateProjectionMatrix();
      };
      resize();
      new ResizeObserver(resize).observe(stage);

      const manager = new THREE.LoadingManager();
      manager.onError = (url) => reportError(`no se pudo cargar ${url.split('/').pop()}`);

      const gltfLoader = new GLTFLoader(manager);
      gltfLoader.load(
        `${base}/models/rosmaster_unified.glb`,
        (gltf) => {
          const robot = gltf.scene;
          robot.rotation.set(0, 0, 0);

          // Ajuste de la cámara RGB-D: 1 cm (10 mm en unidades del CAD) hacia atrás en la referencia del robot
          const rgbdCam = robot.getObjectByName('rgbd_camera');
          if (rgbdCam) {
            rgbdCam.position.z = -10;
          }

          // Ajuste automático de escala con zoom aumentado y centrado
          const box = new THREE.Box3().setFromObject(robot);
          const size = box.getSize(new THREE.Vector3());
          const center = box.getCenter(new THREE.Vector3());
          const maxDim = Math.max(size.x, size.y, size.z);
          const scale = 1.28 / maxDim;
          robot.scale.setScalar(scale);
          robot.position.copy(center).multiplyScalar(-scale);
          robot.position.y -= 0.04;

          scene.add(robot);

          if (hint) {
            hint.classList.add('done');
            setTimeout(() => hint.remove(), 600);
          }

          renderer.setAnimationLoop(() => {
            controls.update();
            renderer.render(scene, camera);
          });
        },
        (progress) => {
          if (!hint || !progress || !progress.loaded) return;
          const mb = (progress.loaded / 1048576).toFixed(0);
          hint.textContent = `cargando robot · ${mb} MB`;
        },
        (err) =>
          reportError(err instanceof ErrorEvent ? err.message : 'no se pudo cargar el modelo 3D')
      );
    }
  }
</script>
