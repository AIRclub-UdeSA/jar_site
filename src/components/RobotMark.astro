---
import { withBase } from '../config/site';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<img
  class:list={['robot-mark', className]}
  src={withBase('/favicon-192x192.png')}
  width="192"
  height="192"
  alt=""
  aria-hidden="true"
/>
